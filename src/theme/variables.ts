import { DEFAULT_THEME as theme } from '@mantine/core';

export const globalColors = {
  app: {
    dark: theme.colors.dark[9],
    light: theme.colors.gray[1],
  },
  code: {
    dark: theme.colors.dark[6],
    light: theme.white,
  },
  paper: {
    dark: theme.colors.dark[7],
    light: theme.white,
  },
  prism: {
    dark: theme.colors.dark[9],
    light: theme.colors.gray[0],
  },
  success: {
    dark: theme.colors.teal[4],
    light: theme.colors.lime[9],
  },
  failure: {
    dark: theme.colors.orange[5],
    light: theme.colors.red[9],
  },
};
